// 変数宣言はconst, let
// とりあえず変数名はconstを使え

alert("Hello World!");

// 配列
const fistPokemons = ["ニャオハ", "ホゲータ", "クワッス"];

console.log(fistPokemons);
console.log(fistPokemons[1]);

// forループ文
// for(初期値; 条件式; 増減){}

const questions = [
    "No.1",
    "No.2",
    "No.3"
]
for(let index = 0; index < questions.length; index++){
    console.log(questions[index]);
}

// >
// >
// >=
// <=
// ===
// !==

// &&
// ||

let isLogin = true;
if(isLogin){
    // alert("ログインしてください")
    
}